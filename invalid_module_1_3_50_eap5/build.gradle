buildscript {
    repositories {
        maven { url "https://dl.bintray.com/kotlin/kotlin-dev" }
        maven { url "https://dl.bintray.com/kotlin/kotlin-eap" }
        maven { url "https://plugins.gradle.org/m2/" }
    }

    dependencies {
        classpath "org.jetbrains.kotlin.multiplatform:org.jetbrains.kotlin.multiplatform.gradle.plugin:1.3.50-eap-5"
    }
}

repositories {
    jcenter()
}

apply plugin: 'org.jetbrains.kotlin.multiplatform'

kotlin {
    targets {
        iosX64("ios")
        configure(ios) {
            binaries {
                framework {
                }
            }
        }
    }
   
}

